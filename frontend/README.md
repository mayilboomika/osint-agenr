# Sherlock Agent Frontend

Open index.html directly in a browser, or serve the folder with:

```bash
python -m http.server 3000
```

The UI calls the backend at http://127.0.0.1:5001/api/verify-credibility.
